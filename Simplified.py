from automate import Automate
# Input format : (weeks)[int]
# Constraints : 2 < weeks < inf
# The program will create a file by the name "output.xlsx", if such a name already exists it will delete the file
# Example Automate(40), no need to worry about files that do not exist the program will handle that automatically
Automate(40)